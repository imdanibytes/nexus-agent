[package]
name = "nexus-anthropic"
version = "0.1.0"
edition = "2021"

[dependencies]
nexus-provider = { path = "../nexus-provider" }
reqwest = { version = "0.13", features = ["stream", "json"] }
bytes = "1"
futures = "0.3"
async-trait = "0.1"
anyhow = "1"
tracing = "0.1"
serde_json = "1"
